#include <sys/types.h>
#include <sys/socket.h>
#include<stdio.h>

//1.socket.c
#if 0
    int socket(int domain, int type, int protocol);
    功能：创建套接字
    domain：确定通信的特性

    type：
    protocol：
    返回值：成功返回套接字描述符，错误返回-1
#endif

int main(){

    return 0;
}
