# Quote 与时间、派生业务边界

本文件名为历史固定入口。第一阶段 Quote 不再是时间权威，不读取或写入 server_time_offset.json，不建立 TIME_QUERY，不处理 1123/1244，不做 UTC 转换。

Quote Binary v1 只保留 MT 原始服务器时间和 Quote 进程接收时间，不包含时间权威、UTC 偏移、Open/Close/Low/High/PrevClosed 或 Owner/Fencing 字段。

时间权威、OpenPrice、M1、归档和历史回填由 Derive/Query 后续方案承接；本轮不修改这些插件运行代码。
