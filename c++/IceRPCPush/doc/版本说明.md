# IceRPCPush 版本说明

---

## 2026-06-17 IceRPCPush 迁移整理与编译告警优化说明

生成时间：2026-06-17 16:35:00

说明：本节记录 `F:\开发资料\MyCode\kds-notes\c++\IceRPCPush` 今日迁移、依赖收敛、公共接口整理、编译告警优化和验证结果。编译中间目录、`.vs`、工程临时文件等不作为功能变更主体。

### 1. 公共入口文件和公共头文件收敛

修改前的问题：

- 旧版入口文件名和新版 DLL 工程入口命名不一致，容易出现 `JsoniRPC.cpp`、`IJsoniRPC.h` 与 `IceRPCPush.cpp`、`IceRPCPush.h` 双入口维护风险。
- 公共头中还残留 `ResponseData`、`JsonBinSrvGetClient` 两个旧声明，但旧版 `.def` 未导出、旧实现也不存在，Visual Studio 会提示缺实现。
- DLL 对外 ABI 需要继续以旧版 21 个导出函数为准。

本次修改内容：

- 将 DLL 对外公共入口统一到 `project\vs2022\IceRPCPush.cpp` 和 `project\vs2022\IceRPCPush.h`。
- 保留旧版 21 个导出函数名、函数签名和 `.def` 导出控制。
- 从 `IceRPCPush.h` 删除未实现、未导出的 `ResponseData` 和 `JsonBinSrvGetClient` 声明。
- `IceRPCPush.h` 继续作为 DLL 对外引用头，保留 `ICERPCPUSH_API` 导入声明；DLL 自身仍通过 `.def` 控制导出名，避免额外 C++ 装饰名导出。

预期效果：

- 外部调用方仍按旧导出函数链接。
- 新工程入口和公共头职责清晰，不再被旧文件名误导。
- Visual Studio 不再因为未实现声明产生无效提示。

### 2. SocketServer、nsdk 依赖收敛到本工程 vender

修改前的问题：

- 工程 include 路径仍引用旁边的 `F:\开发资料\MyCode\kds-notes\c++\SocketServer` 和其 `vender\nsdk`。
- 这会让 IceRPCPush 的构建依赖外部工程目录状态，移植或打包时容易漏文件。

本次修改内容：

- `project\vs2022\IceRPCPush.vcxproj` 的外部 include 路径改为：
  - `vender\SocketServer\include`
  - `vender\nsdk\include`
  - `vender\thread\include`
- 链接库路径确认使用：
  - `vender\SocketServer\lib\x64vc14`
  - `vender\nsdk\lib\x64vc14`
  - `vender\thread\lib\x64vc14`
- 保持链接库名不变：`libSocketServer.lib`、`libnsdk.lib`、`pthreadVC2.lib`。

预期效果：

- IceRPCPush 可以优先依赖本工程 `vender` 下的 SocketServer、nsdk、thread 库。
- 后续迁移、打包和版本归档时依赖来源更明确。

### 3. 第三方和旧 ABI 告警隔离

修改前的问题：

- Ice 3.8.2 头文件会产生大量 `C4251`、`C4275` 告警。
- nsdk 头文件会产生 `C4190` 历史告警。
- 旧协议结构体使用零长度数组，MSVC 会产生 `C4200`。
- tinyxml 静态库缺 PDB 会产生 `LNK4099`。
- 这些告警会淹没本工程真正需要处理的类型转换和格式串问题。

本次修改内容：

- 将 Ice、snappy、json、zlib、tinyxml、SocketServer、nsdk、thread 头目录通过 `/external:I` 作为外部头处理。
- 设置外部头告警级别为关闭。
- 在 `Release|x64` C/C++ 配置中禁用特定告警：
  - `4200`：旧 ABI 零长度数组。
  - `4251`、`4275`：Ice 头文件 DLL 接口类告警。
  - `4190`：nsdk 历史 C 链接返回 C++ 类型告警。
- 链接器增加 `/ignore:4099`，屏蔽 tinyxml 静态库缺 PDB 告警。
- 修正 `OutDir` 末尾斜杠，避免 `MSB8004`。

预期效果：

- 第三方和旧 ABI 噪音从错误列表中移除。
- 本工程真实告警可以被单独定位和修复。

### 4. 手写代码类型转换告警修复

修改前的问题：

- 多处 `size_t` 隐式赋值到 `int`、`long`，产生 `C4267`。
- `long long` 请求号隐式转换到 `DWORD`、`long`、`unsigned short`，产生 `C4244`。
- `SOCKET` 在 x64 下是指针宽度，传入 `int` 会产生截断风险。
- `HCLIENT` 指针句柄在日志中强转 `int`，x64 下会产生 `C4311`、`C4302`。

本次修改内容：

- 在 `src\publicfunc.h` 新增安全转换辅助：
  - `SafeSizeToLength<T>(...)`
  - `SafeLongLongToLength<T>(...)`
- `CompressAByte`、`UnCompressAByte`、RPC/PUT 回包、推送包、订阅包等长度字段统一使用安全转换。
- `Sendn(...)` 的 socket 参数由 `int` 改为 `SOCKET`，避免 x64 截断。
- UDP socket 相关成员改为 `SOCKET`，匹配 Winsock 类型。
- 订阅 map 查询前将 `p_lReqNo` 安全转换为 `DWORD`，避免 map key 隐式收窄。
- TCP 推送日志中的 `HCLIENT` 改为 `%p` 输出，不再转 `int`。

预期效果：

- 手写代码中的 `C4267`、`C4244`、`C4311`、`C4302` 告警清零。
- x64 平台下 socket 和句柄输出不再存在截断隐患。

### 5. 格式串和注释乱码清理

修改前的问题：

- 历史代码把日志系统自定义占位符 `%m_hSocket` 用在 `sprintf(...)` 中，MSVC 会产生 `C4476`、`C4474`。
- 注释中残留旧乱码文本，后续维护容易误判文件编码或业务含义。

本次修改内容：

- 将 `CJsonBinRPCImp.cpp` 中用于 `sprintf(...)` 的 `%m_hSocket` 改为标准 `%s`、`%p`。
- 相关 `sprintf(...)` 改为 `snprintf(...)`，并传入目标缓冲区长度。
- 日志类自身仍保留对 `%m_hSocket` 的兼容处理，不影响原日志调用。
- 清理 `JsonICEResponseData` 附近乱码注释，改成正常中文说明。
- 清理注释中的旧 `sprintf("%m_hSocket...")` 示例，避免扫描误报。

预期效果：

- `C4476`、`C4474` 告警清零。
- 源码中文注释不出现常见乱码标记。

### 6. 编译产物和导出表验证

验证命令：

- `msbuild project\vs2022\IceRPCPush.sln /t:Rebuild /p:Configuration=Release /p:Platform=x64`
- `dumpbin /exports lib\x64vc14\libIceRPCPush.dll`

验证结果：

- `Release|x64` 重新构建成功。
- `0` 个错误。
- `0` 个告警。
- DLL 导出表保持 `21` 个函数。
- 旧版核心导出仍存在：
  - `CreateJsonICEServer`
  - `CreateJsonICEClient`
  - `CreateJsonICEClient2`
  - `RegisterJsonICEClient`
  - `RegisterJsonICEClient2`
  - `JsonBinClientRPC`
  - `JsonBinClientRPC_async`
  - `JsonBinClientPUT`
  - `JsonBinClientPUT_async`
  - `PreJsonBinClientRPC`
  - `EndPreJsonBinClientRPC`
  - `PreJsonBinClientPUT`
  - `EndPreJsonBinClientPUT`
  - `PushJsonICEServerData`
  - `JsonICEResponseData`
  - `DeleteJsonICERPC`

### 7. 扫描校验结果

本次额外做了以下扫描：

- 扫描 `ResponseData`、`JsonBinSrvGetClient` 公共声明残留。
- 扫描 `sprintf/snprintf/_snprintf` 中是否仍使用 `%m_hSocket`。
- 扫描常见中文乱码标记。

结果：

- 未发现 `ResponseData`、`JsonBinSrvGetClient` 的公共声明残留。
- 未发现 `sprintf(...)`、`snprintf(...)`、`_snprintf(...)` 继续使用 `%m_hSocket`。
- 未发现常见中文乱码标记。

### 提交前注意

- `lib\x64vc14\libIceRPCPush.dll`、`lib\x64vc14\libIceRPCPush.lib`、`lib\x64vc14\libIceRPCPush.pdb` 已随 Rebuild 重新生成。
- 当前工程依赖已收敛到 `IceRPCPush\vender` 下的 SocketServer、nsdk 和 thread，后续更新这些库时应同步替换对应 `include`、`lib`、`dll`。
- `src\Corba\JSONBINRPCU.cpp/.h` 是 `slice2cpp.exe` 生成文件，本轮未修改生成文件业务内容。
- 后续功能回归建议重点覆盖：
  - 服务端创建和销毁。
  - 客户端创建和注册/注销推送。
  - 同步 RPC/PUT。
  - 异步 RPC/PUT。
  - Pre/End RPC/PUT。
  - TCP/UDP/Ice 推送路径。

## 2026-06-23 IceRPCPush 底层网络库稳定性与工程配置修复

### 1. 底层异常捕获和错误查询能力

涉及文件：
- `project/vs2022/IceRPCPush.h`
- `src/IceRPCPush.cpp`
- `src/CJsonBinRPCImp.cpp`

本次修改内容：
- 在 DLL 导出边界和关键 Ice 调用路径增加异常保护，捕获 Ice 异常、标准异常和未知异常。
- 新增最后错误查询导出接口：`IceRPCPushGetLastErrorCode`、`IceRPCPushGetLastError`、`IceRPCPushGetErrorMsg`。
- 错误码和错误描述统一使用英文，便于上层 DLL 或业务插件按错误码判断失败原因。
- 底层失败时同步记录日志，日志包含函数名、错误码和具体英文错误描述。

预期效果：
- 调用方不再只能通过空句柄或空结果猜测失败原因。
- Ice 连接失败、RPC/PUT 调用异常、推送注册失败等路径都能查询到明确错误。

### 2. PUT 回调、压缩和推送路径修复

涉及文件：
- `src/CJsonBinRPCImp.cpp`
- `src/CJsonBinRPCImp.h`

本次修改内容：
- 修复 `JsonBinPUT_async` 中解压参数误用问题。
- 补齐 PUT 直回调模式，保证上层使用回调服务模式时 RPC 和 PUT 都能收到请求。
- 直回调请求数据改为内部深拷贝，避免把局部缓冲区指针传给上层造成悬空访问。
- `CompressAByte`、`UnCompressAByte` 增加空数据和失败路径保护，避免空 vector 取地址。
- `JsonICEResponseData` 统一使用压缩辅助函数，降低手工估算压缩缓冲区不足的风险。
- 推送回调列表改为锁内复制、锁外调用，减少注册/注销与回调互相等待的死锁风险。

预期效果：
- 服务端回调模式可以稳定处理 RPC 和 PUT。
- 推送和回包路径减少悬空指针、压缩失败和回调重入风险。

### 3. 锁和结构体规范整理

涉及文件：
- `src/AutoCS.h`
- `src/DQueue.h/.cpp`
- `src/UserManage.h/.cpp`
- `src/userdata.h`

本次修改内容：
- 引入基于 `vender/xsdk` 的互斥锁封装思路，低风险模块优先改为 RAII 锁保护。
- 手写结构体成员去掉 `m_` 前缀，保留类成员 `m_` 前缀。
- 结构体字段补充中文注释，并在构造函数中初始化默认值。
- 保持生成文件 `JSONBINRPCU.*` 和第三方代码不修改。

预期效果：
- 队列、用户管理等基础模块锁释放更稳定，异常路径不容易遗漏解锁。
- 手写结构体命名和注释符合当前工程规范。

### 4. 工程配置和导出表修复

涉及文件：
- `project/vs2022/IceRPCPush.vcxproj`
- `include/IceRPCPush.def`

本次修改内容：
- 修复链接器模块定义文件路径，将 `IceRPCPush.def` 改为 `..\..\include\IceRPCPush.def`。
- 解决 `LINK : fatal error LNK1104: 无法打开文件“IceRPCPush.def”` 的构建失败问题。
- 保持 DLL 导出名由 `.def` 控制，避免新增 C++ 装饰名。

验证结果：
- 已执行 `Release|x64` 构建。
- `0` 个警告。
- `0` 个错误。
- 输出文件：`lib/x64vc14/libIceRPCPush.dll`。
